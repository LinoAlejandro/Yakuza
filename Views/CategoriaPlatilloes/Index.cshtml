@model IEnumerable<Yakuza.Models.CategoriaPlatillo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreCategoriaPlatillo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcionCategoriaPlatillo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreCategoriaPlatillo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcionCategoriaPlatillo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCategoriaPlatillo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCategoriaPlatillo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCategoriaPlatillo })
        </td>
    </tr>
}

</table>
